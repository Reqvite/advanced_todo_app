import {Box} from '@chakra-ui/layout';
import {TodoTable} from '@/components/todoTable';

const MainPage = () => {
  return (
    <Box>
      <TodoTable />
    </Box>
  );
};

export default MainPage;
